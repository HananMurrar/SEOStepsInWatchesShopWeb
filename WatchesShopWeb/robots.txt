User-agent: *
Allow: /
Sitemap: sitemap.xml

# the robots.txt tells search engines which pages to crawl or ignore

# User-agent: * → means all search engines
# Allow: / → means they can see everything
# Sitemap → points search engines to your sitemap
